# DramaTV 项目工程理解 错题与遗漏

## 活跃遗漏

| 日期 | 来源课程 | 遗漏点 | 原回答问题 | 正确理解 | 下次复习重点 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-21 | 03_后端运行基础：微服务、JVM、线程池与连接池 | 300 个用户同时访问不等于新建 300 个线程，原因需要巩固 | “不会，原因不是很确定” | Spring Boot 默认内嵌 Tomcat，Tomcat 请求线程池会复用线程；线程不够时请求排队，超出承载后可能超时或拒绝 | 并发用户数、处理中请求数、线程池上限三者不是同一个概念 | 活跃 |
| 2026-06-21 | 03_后端运行基础：微服务、JVM、线程池与连接池 | Tomcat 请求线程池和数据库连接池区别不够清楚 | “不是一个东西，区别不清楚” | Tomcat 请求线程池控制 HTTP 请求处理并发；数据库连接池控制同时借出多少数据库连接执行 SQL | 用精选页请求链路复述“请求线程借连接、查完归还连接”的过程 | 活跃 |
| 2026-06-21 | 03_后端运行基础：微服务、JVM、线程池与连接池 | 容易把“同时检查数据库连接池”理解成“必须同时调大连接池” | “需要同时增大数据库连接池” | 连接池是否调大取决于数据库是否还有承载余量；如果 SQL 慢或数据库已满载，盲目加连接会更慢 | 先定位瓶颈，再决定优化 SQL、分页、缓存、Tomcat 线程池或数据库连接池 | 活跃 |
| 2026-06-21 | 04_本地到云端运行链路：端口、域名、Nginx、Spring Boot、数据库与OSS | 云端访问链路里漏掉了前台服务层和 `/media/**` 代理层 | “浏览器 -> Nginx -> 云后端 -> 云数据库，redis” | 真实链路通常是“浏览器 -> 域名 -> Nginx -> 前台服务；页面中的 `/api/**` 与 `/media/**` 再分别进入后端 -> 数据库/Redis/OSS” | 区分“页面入口链路”和“页面里的数据/媒体链路” | 活跃 |
| 2026-06-21 | 04_本地到云端运行链路：端口、域名、Nginx、Spring Boot、数据库与OSS | 已能识别裸 IP 不对可能是 Nginx 转发问题，但还要补 Host 分流和同机其他项目因素 | “可能是nginx 转发问题” | 裸 IP 打开不是社区，不一定是社区挂了，也可能是 Host 没命中社区 server_name、被同机其他项目兜底吃掉、历史入口口径已变化 | 入口层故障要同时看域名、Host、Nginx、同机项目分流 | 活跃 |
| 2026-06-21 | 04_本地到云端运行链路：端口、域名、Nginx、Spring Boot、数据库与OSS | 回滚理解方向对，但要补“不是所有数据都能靠软删除 + SQL 恢复” | “真要回滚可以通过写sql解开” | 代码 release 可回滚，但 migration、运营配置、外部状态、OSS 对象不一定能完整逆向恢复；写 SQL 只是部分补救手段，不是标准回滚能力 | 区分代码回滚、数据修复、外部状态补偿三类动作 | 活跃 |

## 已解决遗漏

| 解决日期 | 来源课程 | 原遗漏点 | 解决证据 |
| --- | --- | --- | --- |
| 2026-06-21 | 04_本地到云端运行链路：端口、域名、Nginx、Spring Boot、数据库与OSS | 本地 `3106 / 3107 / 18080` 作用混淆风险 | 用户已能回答：`3106` 是本地前台，`3107` 是面向云资源链路的前台镜像，`18080` 是后端服务端口 |

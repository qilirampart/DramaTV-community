对，我也看到了，.codex/progress.md 现在已经混了两条线：

  - 社区前台主线
  - 社区后台 apps/admin 并行线

  所以现在不能再直接按 progress.md 的时间顺序判断优先级，容易被后台开发日志带偏。更稳的做法是把任务按战
  线拆开看。

  我对当前状态的判断
  真正还没收口的，不是“再多做几个页面”，而是这 5 条主线：

  1. 发布状态模型收口
  2. 媒体链路稳定化
  3. 分类体系后端化
  4. 登录/会话/权限边界收口
  5. 后台治理能力落地

  按优先级排，社区现在最该做什么

  1. 先收口“发布状态四层模型”
     现在文档里已经明确下一步要把 draft / moderation / processing / visibility 分开，这个优先级最高。
     不做这一步，前台、后台、通知、统计、作者页、精选页都会继续互相串状态。
  2. 把媒体链路做成稳定闭环
     现在已经补了：

  - 大文件上传代理
  - 自动抽封面
  - poster 回填
  - preview 压缩
  - 云端 ffmpeg

  但还没彻底闭环：

  - 损坏视频怎么处理
  - 处理中内容前台怎么展示
  - 任务失败怎么查
  - 后台怎么重试
  - OSS / CDN / Range 播放如何正式收口

  3. 把分类体系从“前端规则”升级成“后端正式字段”
     现在前端 taxonomy 已经有了，但如果后端还主要靠 tag_names / modelName / 导入脚本猜测，后面会继续
     乱。
     这个会同时影响：

  - /featured 筛选
  - 发布页标签选择
  - 历史资源导入
  - 后台分类治理
  - 后续搜索/推荐

  4. 做一轮登录态与多用户隔离专项验证
     你之前已经碰到过公网登录态、上传、受保护路由、跳登录、黑屏 loading 这类问题。
     这条线现在虽然“基本可用”，但还没到“放心让多人长时间测”的程度。
     重点要验证：

  - 不同浏览器/不同账号 cookie 是否隔离
  - 登录后重定向是否稳定
  - 上传代理是否在所有入口都一致
  - 后续切公司画布登录时，当前方案能否平滑替换

  5. 后台不要只做页面，要继续接真实治理接口
     后台这边现在已经有：

  - auth
  - users
  - 一些页面 UI 骨架和交互修复

  但真正决定是否可运营的是：

  - 内容审核中心
  - 举报处理中心
  - 评论治理
  - 媒体/异步任务中心
  - 运营排序中心

  这些如果只做页面、不接真实 /api/admin/**，后面仍然会回到手改数据。

  最近问题可能联动出的衍生问题

  1. 发布状态一旦拆开，会连带影响这些地方：

  - 首页 /home
  - 精选 /featured
  - 视频/提示词/工作流详情
  - 作者页 /creators/[id]
  - 个人中心 /me
  - 后台审核列表
  - 通知与统计数字

  2. 媒体处理一旦异步化，会连带出现这些问题：

  - 刚发布时封面还没生成，卡片显示什么
  - preview 还没生成，详情页优先播什么
  - 任务失败时用户看见什么
  - 后台如何看到 cover/poster/preview/source 的缺口
  - 公网 ECS 上跑压缩是否会抢 API 资源

  3. 分类体系一旦正式化，会连带影响：

  - 精选页 query 参数和筛选逻辑
  - 发布页表单字段
  - 导入脚本映射规则
  - 后台 taxonomy 管理
  - 搜索、推荐、运营位排序口径

  4. 登录态一旦切公司画布体系，会连带影响：

  - 前台社区登录
  - 上传代理的鉴权方式
  - /me
  - 画布入口
  - 后台 RBAC
  - 跨域 cookie / token 传递策略

  5. 后台和社区共用后端后，会连带要求更严格的边界：

  - /api/** 和 /api/admin/** 权限必须完全分开
  - 审核/下线/恢复操作要有审计日志
  - 后台改排序后，前台立刻能看到
  - 媒体失败任务不能只靠研发上服务器查

  1. 社区主线

  - 发布状态模型
  - 媒体闭环
  - 登录态稳定性
  - 前台性能回归

  2. 分类与数据

  - taxonomy 正式字段
  - 精选筛选口径统一

  3. 后台治理

  - admin auth / users
  - moderation
  - reports
  - comments
  - media tasks
  - feed ops

  4. 云化与运维

  - OSS
  - CDN
  - Range
  - 日志采集
  - traceId 链路
  - 本地/云环境一致性

  如果只选一个最该先做的
  我会先做“发布状态模型收口 + 对应回归测试”。

  因为它是后面这几条线的共同地基：

  - 前台展示是否提前泄露
  - 后台审核是否有意义
  - 通知是否准确
  - 统计是否可信
  - 媒体处理中的内容是否该公开

  如果你愿意，我下一步可以直接帮你把当前任务重新整理成一份“社区主线 / 后台并行线 / 云化线”的清晰待办文
  档，替代现在这个有点混的总进度账本。
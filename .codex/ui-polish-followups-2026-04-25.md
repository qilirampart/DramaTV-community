# UI 精修待办清单（2026-04-25）

来源：本次会话中用户明确提出的后续精修需求。

使用规则：
- 当前文件只用于记录和跟踪，不代表已开始修改代码。
- 后续每完成一个任务，同时更新本文件和 `.codex/progress.md`。
- 状态规则：`[ ]` 未完成，`[x]` 已完成。
- 为避免重复处理，任务只在这里维护一份主清单。

## 当前待办

- [x] D1 详情页评论区主题统一为黑白风格
  - 问题：详情页评论区目前仍带黄色系，不符合当前主页主风格。
  - 目标：改成与主页一致的黑白体系。
  - 范围：评论输入框、发布按钮、提示文案、分隔线、评论列表相关辅助色。
  - 注意：夜间模式和日间模式都要一起检查，不能只改暗色。

- [x] D2 日间模式正文颜色修正
  - 问题：详情页正文在日间模式下仍偏白，阅读性不对。
  - 目标：正文、正文说明、辅助文案在日间模式下统一切为黑色或深灰色。

- [x] D3 日间模式预览区雾感清理
  - 问题：预览区仍有一层背景色叠加的雾蒙蒙感觉，下半部分更明显。
  - 目标：去掉不必要的蒙层感，让预览区更干净。
  - 排查重点：背景渐变、遮罩层、全局 glow、卡片 overlay、透明度叠加。

- [x] D4 一级分类显示资源数量
  - 问题：当前顶部筛选项只有名称，没有展示每类资源数量。
  - 目标：为 `全部 / 工作流 / 视频提示词 / 图片提示词 / 活动` 增加数量显示。

- [x] D5 一级分类下补充二级主题分类
  - 问题：当前只有工作流、视频提示词、图片提示词等大类，缺少每类下面的主题细分。
  - 目标：在一级分类之下增加资源主题小分类。
  - 范围：
    - 工作流二级主题分类
    - 视频提示词二级主题分类
    - 图片提示词二级主题分类
  - 约束：分类要结合现有真实数据，避免做成空分类或伪分类。

## 新增待办（2026-04-26）

- [x] D6 3107 顶栏最右上角多余入口移除
  - 问题：`/home` 顶栏最右上角仍有一个额外的小方形入口，和当前导航结构不匹配，用户截图中已明确要求去掉。
  - 目标：移除该冗余入口，保留现有主导航、通知、主题切换、个人入口即可。
  - 排查入口：
    - `apps/web/src/components/shared/PageShell.tsx`
    - `apps/web/src/app/globals.css`

- [x] D7 3107 顶栏用户头像缺失修复
  - 问题：顶部用户区出现空白圆角占位，头像没有正常显示，fallback 视觉也不明确。
  - 目标：无论是否有头像资源，都要稳定显示“头像图”或“明确的 fallback”。
  - 排查入口：
    - `apps/web/src/components/shared/PageShell.tsx`
    - `apps/web/src/app/globals.css`

- [x] D8 个人主页“作品”卡片样式统一
  - 问题：个人主页作品区当前存在多套卡片视觉，用户要求统一成截图中第三条作品卡片的样式方向。
  - 目标：统一作品卡片的封面比例、信息层级、背景语气和间距，不再混用不同风格。
  - 排查入口：
    - `apps/web/src/features/me/PersonalCenterPage.tsx`
    - `apps/web/src/features/me/PersonalCenterPage.module.css`
    - `apps/web/src/features/creator/CreatorPage.tsx`
    - `apps/web/src/features/creator/CreatorPage.module.css`

- [x] D9 个人主页“帖子”卡片样式统一
  - 问题：帖子区当前和作品区一样存在样式割裂，展示密度、边框感和信息排布不统一。
  - 目标：帖子卡片统一成同一套展示体系，并和当前个人主页整体风格保持一致。
  - 排查入口：
    - `apps/web/src/features/me/PersonalCenterPage.tsx`
    - `apps/web/src/features/me/PersonalCenterPage.module.css`
    - `apps/web/src/features/creator/CreatorPage.tsx`
    - `apps/web/src/features/creator/CreatorPage.module.css`

- [x] D10 DramaTV 顶栏品牌链接补充悬浮反馈
  - 问题：`Drama TV` 可点击，但鼠标悬浮时几乎没有反馈，交互感不足。
  - 目标：补充和当前站点风格一致的 hover / focus-visible 反馈，不做夸张动效。
  - 排查入口：
    - `apps/web/src/components/shared/PageShell.tsx`
    - `apps/web/src/app/globals.css`

- [x] D11 发帖页日间模式配色修正
  - 问题：帖子发布页在日间模式下仍沿用夜间底色与文字体系，阅读和编辑状态不对。
  - 目标：让 `/discussions/new` 的日间模式回到白底黑字主系，同时保持现有页面结构和社区气质。
  - 排查入口：
    - `apps/web/src/features/discussions/DiscussionComposerPage.tsx`
    - `apps/web/src/features/discussions/DiscussionComposerPage.module.css`

## 新增待办（2026-04-27）

- [x] D12 `/me` 与创作者页作品卡片封面层统一
  - 问题：个人主页作品卡片在“有封面 / 无封面”两种情况下底图层逻辑不一致，导致同一排卡片风格发散。
  - 目标：把封面图改成独立 cover layer，保留统一的底色、遮罩和卡片壳，避免出现有的偏暖、有的偏黑的割裂感。
  - 额外处理：顺手把 `/me` 作品网格的尺寸、间距和文字层级继续向创作者页卡片体系收口。

- [x] D13 讨论区转义 Unicode 文案解码
  - 问题：讨论卡片标签出现 `\\U7F6E\\U9876\\U8BA8\\U8BBA` 这类原始转义文本，直接暴露给用户。
  - 目标：在前端展示层统一解码这类 `\\uXXXX / \\UXXXX` 文本，避免讨论标题、标签、频道名继续出现同类问题。
  - 范围：共享文本标准化显示层。

- [x] D14 视频详情页“收起视频”按钮避让浏览器原生控件
  - 问题：按钮放在右下角时会和浏览器原生全屏入口重叠，影响全屏操作。
  - 目标：改到右上角，保留操作可见性，同时不遮挡原生视频控件。

- [x] D15 讨论区置顶标签双重转义修复
  - 问题：讨论区首条帖子右侧的“置顶讨论”标签仍显示为 `\U7F6E...` 原始转义串。
  - 根因：这不是后端字段问题，而是前端页面里该标签本身落成了错误的双重转义文本。
  - 目标：直接改回正常中文标签，并继续保留共享显示层的转义文本兜底解码。

- [x] D16 视频详情页主按钮语义改为“开始播放 / 暂停播放”
  - 问题：之前这颗按钮的实际效果更像“切有声层 / 切静音层”，用户理解上等同于开关声音，不是播放控制。
  - 目标：改为真正驱动主视频的播放/暂停状态；首次点击进入播放态，再点切暂停，按钮文案同步更新。

- [x] D17 `/me` 与创作者页卡片暖色残留收口
  - 问题：个人主页当前“作品 / 帖子”卡片仍带明显土黄色、棕黄色渐变与徽标色，和站点主黑白基调不一致。
  - 目标：统一把 `/me` 与 `/creators/[id]` 两套卡片壳层、遮罩、徽标收口为黑白中性色，保留现有排版与信息密度，不再出现偏暖卡片底色。
  - 排查入口：
    - `apps/web/src/features/me/PersonalCenterPage.module.css`
    - `apps/web/src/features/creator/CreatorPage.module.css`
  - 完成情况：
    - `/me` 作品卡与帖子卡的底层渐变已从暖棕改为石墨黑灰，徽标改为深灰底白字
    - `/creators/[id]` 的作品卡与帖子卡同步收口为同一套黑白卡片语气
    - 已重启 `3106` 与 `3107`，并在浏览器中确认相关卡片 `backgroundImage / badge` 计算样式已切到黑白中性色

- [x] D18 个人主页作品区与精选页工作流口径对齐
  - 问题：当前作品区混入了“绑定工作流的视频样片”，导致作者页显示成 4 条内容；但按现阶段产品口径，作品区一级类只认 `提示词` 和 `工作流`，不单独把工作流样片算成第三类内容。
  - 目标：作者页作品区按 `提示词 / 工作流` 两类收口；绑定在工作流下的视频样片不再单独占一条，只保留真实提示词作品与工作流本体。
  - 排查入口：
    - `apps/web/src/features/me/PersonalCenterPage.tsx`
    - `apps/web/src/features/creator/CreatorPage.tsx`
  - 完成情况：
    - `/me` 与 `/creators/[id]` 的作品区已过滤掉绑定工作流的视频样片
    - 当前这位作者的作品数已从 4 收口为 2，并与精选页的工作流口径一致

- [x] D19 个人主页作品卡固定四列尺寸
  - 问题：当作者页作品数不足一行时，卡片会被 `auto-fit` 横向拉宽，和站内“一行四个”的展示基线不一致。
  - 目标：桌面端固定按四列基线展示，中屏降列，小屏单列；数据不足一行时也保持标准卡片宽度，不再横向撑大。
  - 排查入口：
    - `apps/web/src/features/me/PersonalCenterPage.module.css`
    - `apps/web/src/features/creator/CreatorPage.module.css`
  - 完成情况：
    - `/me` 作品网格已改为桌面端固定四列
    - 创作者页作品网格同步改为固定列宽策略

## 建议处理顺序

1. 先处理 `D1 + D2 + D3`
  - 这一组都属于视觉体系和日间模式统一问题，适合一轮收口。

2. 再处理 `D4 + D5`
   - 这一组属于精选页信息结构增强，适合在视觉问题稳定后继续推进。

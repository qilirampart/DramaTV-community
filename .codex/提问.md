## 提问

老师你好，我在推进 DramaTV 社区项目，准备先把测试环境的媒体上传
  接入 OSS。

  目前拿到的信息是：
  - Bucket：dz-ailab-community
  - OSS 域名：dz-ailab-community.oss-cn-beijing-internal.aliyuncs.com

  我们这边想确认几个信息，方便开始联调：

  2. 当前给到的是 `internal` 域名。这个域名我们理解是给云上服务内网访问 OSS
       用的，想确认是否正确？

  3. 我们后端接入 OSS 时，认证方式是走 `RAM Role`，还是提供 `AK/SK`？

  4. 如果走 `AK/SK`，测试环境这边是否可以提供对应的 `AccessKeyId /
       AccessKeySecret`？

  5. 如果走 `RAM Role`，当前测试服务器是否已经绑定可用 Role？如果已绑定，麻
       烦同步一下 Role 名称和权限范围。

  6. 除了这个 `internal` 域名外，是否还有给前端页面展示图片/视频使用的公网域
       名、自定义域名或 CDN 域名？

  7. 这个 Bucket 当前权限是私有读写，还是公网可读？

  8. 如果后续我们本地开发阶段也需要联调 OSS，是否需要额外开白名单或提供公网
       访问方案？

       ## 回答

2.是正确的
3.RAM Role，直接授权给ecs的
5.应该是读写权限。
6.cdn还没有配置，是需要前端展示吗？
7.私有读写的，不能放到公网上